# Core dependencies
ollama>=0.1.0
pandas>=2.0.0

# Note: asyncio, argparse, pathlib, uuid, and logging are part of Python's standard library
# and don't need to be included in requirements.txt

# Optional dependencies for development
pytest>=7.0.0
black>=23.0.0
flake8>=6.0.0
mypy>=1.0.0

# Logging extensions (optional)
python-json-logger>=2.0.0 